sudo: required

language: java

services:
  - docker

before_install:

- echo $USER

# get IP Address for remote selenium grid 
- ifconfig
    
script:
- xvfb-run --server-args="-screen 0 1024x768x24" make test

- mvn clean test